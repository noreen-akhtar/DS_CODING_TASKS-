//Suppose you are given a string: ‘Karachi’. Convert it to ‘Kolachi’ (Hint: Need to look at appropriate indices and replace them)
let  str = "karachi";
 let   str1 = str.replace('karachi', 'kolachi');
console.log(str1);